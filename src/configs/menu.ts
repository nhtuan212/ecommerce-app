import { ROUTER } from "@/configs/router";
import { TEXT } from "@/constants/text";

export const MenuConfig = [
    {
        id: "menuId",
        name: TEXT.PRODUCT,
        slug: ROUTER.PRODUCT,
    },
];
