query GetCollections {
    collections(channel: "default-channel", first: 10) {
        edges {
            node {
                id
                slug
                name
            }
        }
    }
}